"use strict";
const sum = (x, y) => {
    return x + y;
};
const sub = (x, y) => {
    return x - y;
};
console.log(sum(3, 4));
const log = (message) => {
    console.log(message);
};
log('Hello There!');
